import Nav from '@/components/organisms/layout/Nav.svelte';
import type { Meta } from '@storybook/svelte';

export default {
  title: 'Nav',
  component: Nav
} satisfies Meta;

export const Primary = {};
